<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="icon" href="/favicon.ico?v=2" type="image/x-icon" />
  <link href="https://unpkg.com/tailwindcss@^2/dist/tailwind.min.css" rel="stylesheet" />
  <link rel="stylesheet"
    href="https://maxst.icons8.com/vue-static/landings/line-awesome/line-awesome/1.3.0/css/line-awesome.min.css">
  <link rel="stylesheet" href="/css/app.css" />
</head>

<body>
  <nav class="sticky top-0">
    <div class="mx-auto flex items-center justify-between py-4 container pl-3 md:pl-0">
      <a class="hidden md:contents" href="/">
        <img src="/img/logo.png" alt="logo" /></a>
      <div>
        <ul class="flex items-center">
          <% if(user) { %>
            <li class="ml-6 hidden md:contents">
              Welcome,
              <%= user.name %>
            </li class="ml-6">
            <% } %>
              <li class="ml-6">
                <a href="/#menu">Menu</a>
              </li class="ml-6">
              <li class="ml-6">

                <% if(user) {%>
                  <% if(user.email==="admin@admin.com" ) {%>
                    <a href="/admin/orders">Orders</a>
                    <%} else {%>
                      <a href="/customer/orders">Orders</a>
                      <% } %>
                        <% } %>

              </li class="ml-6">
              <% if(user) { %>
                <li class="ml-6">
                  <a href="/auth/logout">Log out</a>
                </li class="ml-6">
                <% } else { %>
                  <li class="ml-6">
                    <a href="/auth/login">Login</a>
                  </li class="ml-6">
                  <li class="ml-6">
                    <a href="/auth/register">Register</a>
                  </li class="ml-6">
                  <% } %>
                    <li class="ml-6">
                      <a href="/cart" class="inline-block px-4 py-2 rounded-full flex">
                        <img src="/img/cart.png" alt="" />
                        <span id="cartCouter" class="text-white font-bold pl-2">
                          <%= session.cart ? session.cart.totalQty : '' %>
                        </span>
                      </a>
                    </li class=" ml-6">
        </ul>
      </div>
    </div>
  </nav>

  <%- body %>
    <footer class="text-center py-10 text-lg">
      Copyright © 2021 Tran Huu Toan
    </footer>
    <script src="/js/app.js"></script>
</body>

</html>